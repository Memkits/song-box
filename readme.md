
### song box is a personal Chrome only simple music player

To use it, put you `.mp3` songs in `mp3/dir`, and  
`cd` to `mp3/` and run `livescript list.ls` to write index.
Then use Nginx to server the files.. use Chrome to run it..